spring.application.name=GradeTrackerSpringBoot

# Disable Thymeleaf cache during development; remove or set to true for production
spring.thymeleaf.cache=false

# Optional: change port if 8080 is already in use
# server.port=8081